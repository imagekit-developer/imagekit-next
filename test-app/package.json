{
  "private": true,
  "scripts": {
    "clean": "rm -rf .next/",
    "dev": "npm run clean && next dev",
    "build": "next build",
    "start": "npm run clean && npm run build && next start",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "next": "latest",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "20.10.4",
    "@types/react": "18.2.45",
    "typescript": "5.3.3"
  }
}
